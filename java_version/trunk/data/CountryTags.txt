#Many to one mapping of country tags.

#Format: link = { (eu3 = _eu3tag_ )+ vic = _victag_ }
#Note that converter automatically translates unknown EU3 tags to same Vic2 tag,
#so it is not necessary to add conversions like SWE => SWE.

#As for now the converter rather arbitrarily decides what source country
#should represent the target country at merge.

#link = { eu3 = SWE eu3 = ENG vic = RUS } non-sensical example
